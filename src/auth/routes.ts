export const authRoutes = ['/auth/login', '/auth/register'];
export const publicRoutes = ['/'];
export const privateRoutes = ['/dashboard'];
export const userRoutes = ['/checking'];

export const DEFAULT_REDIRECT = '/dashboard';
